refresh_rate: 30 seconds
appenders:
  console:
    kind: console
    encoder:
      pattern: "{h({d(%Y-%m-%d %H:%M:%S %Z)(utc)} [{l}] {t} - {m})}{n}"
  rolling_file:
    kind: rolling_file
    path: /var/log/lizt/lizt.log
    encoder:
      pattern: "{h({d(%Y-%m-%d %H:%M:%S %Z)(utc)} [{l}] {t} - {m})}{n}"
    policy:
      kind: compound
      trigger:
        kind: time
        interval: 1 day
      roller:
        kind: fixed_window
        pattern: /var/log/lizt/lizt.{}.log.gz
        base: 1
        count: 8

root:
  level: warn
  appenders:
    - console
    - rolling_file

loggers:
  web:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  pipeline:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  io_nvd:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  io_symbols:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  io_inventory:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  db:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file
  monitor:
    level: debug
    additive: false
    appenders:
      - console
      - rolling_file